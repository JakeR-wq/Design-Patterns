package strategy;

// implementation of the play() function for chasing the puck defence behavior
public class ChasePuckBehavior implements DefenceBehavior {
    public String play() {
        return "Chases the puck";
    }
}
